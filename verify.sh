#!/bin/sh

TODO="write a verification script that uses strace to trace and analyze system calls and detect the execution of a shell-spawning payload injected by the compromised crt0.o"

printf "%s\n" "$TODO"
